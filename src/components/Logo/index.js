import React from 'react'

const Logo = props => {
    return (
        <span className={props.classLogo}>Github
        <span className={props.classLogoItalic}> Search</span></span>
    )
}

export default Logo